var cover = document.getElementById("cover");

/*window.onload = function () {
  var actual = window.location + "";
  var split = actual.split("#");
  var id = split[split.length - 1];
  if (id == "projects") {
    hideCover();
  } else {
    showCover();
  }
};*/

/*function showCover() {
  cover.style.top = 0;
}

function hideCover() {
  cover.style.top = "-100%";
}

const body = document.body;
let lastScroll = 0;

window.addEventListener("scroll", () => {
  const currentScroll = window.pageYOffset;
  if (currentScroll <= 0) {
    showCover();
    return;
  }

  if (currentScroll > lastScroll && !body.classList.contains(cover.style.top == 0)) {
    hideCover();
  } else if (
    currentScroll < lastScroll &&
    body.classList.contains(cover.style.top == 0)
  ) {
    showCover();
  }
  lastScroll = currentScroll;
});*/